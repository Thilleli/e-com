{% extends 'base.html.twig' %}

{% block title %}Produits de la catégorie {{ category.namename }}{% endblock %}

{% block body %}
    <h3 class="title-primary">Produits de la catégorie {{ category.namename }} </h3>
    <div class="row_product">
        <div class="product-container">
            {% for product in products %}
                <div class="card" style="width: 18rem;">
                    <img src="/upload/images/{{ product.fileName.path }}" class="card-img-top" alt="">
                    <div class="card-body">
                        <h5 class="card-title">{{ product.name }}</h5>
                        <p class="card-text">{{ product.priceHT }} €</p>
                        <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn btn-primary">Voir détails</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
