{% extends 'base.html.twig' %}

{% block title %}Images{% endblock %}

{% block body %}
    <h1>Images</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ image.path }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ image.name }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ image.alt }}</td>
            </tr>
            <tr>
                <th>Aperçu</th>
                <td>
                    <img src="{{ image.path }}" alt="{{ image.alt }}" style="width: 100px; height: 100px; object-fit: cover;">
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_images_index') }}">back to list</a>

    <a href="{{ path('app_images_edit', {'id': image.id}) }}">edit</a>

    {{ include('images/_delete_form.html.twig') }}
{% endblock %}
